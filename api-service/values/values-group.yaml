replicaCount: 2

image:
  repository: docker.io/chat-app/group-service
  tag: latest
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 8082
  containerPort: 8080

env:
  - name: SPRING_PROFILES_ACTIVE
    value: prod
  - name: MONGO_URI
    value: mongodb://mongo:27017/chat_app
  - name: KAFKA_BROKER
    value: kafka:9092

ingress:
  enabled: true
  host: group.chat-app.local
